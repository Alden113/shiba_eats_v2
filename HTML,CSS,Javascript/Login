<!DOCTYPE html>
<html lang="zh-Hant-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="https://cdn-icons-png.flaticon.com/128/8708/8708467.png" type="image/x-icon"> 
    <!--// icon來源:https://www.flaticon.com/search?word=shiba by Paul J.-->
    <title>歡迎來到Shiba Eats!</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('https://khh.travel/content/images/static/4-1-food-11.jpg') no-repeat center center fixed;
            background-size: cover;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }
        .container h2 {
            margin-bottom: 40px;
        }
        .container form {
            display: flex;
            flex-direction: column;
        }
        .container form input {
            margin-bottom: 20px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 8px;
        }
        .container form button {
            padding: 10px;
            border: none;
            border-radius: 4px;
            background-color: #28a745;
            color: white;
            cursor: pointer;
        }
        .container form button:hover {
            background-color: #218838;
        }
        .container .toggle-link {
            margin-top: 10px;
            color: #007bff;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container" id="login-container">
        <h2>會員登入</h2>
        <form id="login-form">
            <input type="text" id="login-username" placeholder="會員名稱" required>
            <input type="password" id="login-password" placeholder="密碼" required>
            <button type="button" onclick="login()">登入</button>
            <p class="toggle-link" onclick="toggleForm()">還沒註冊? 點此註冊</p>
        </form>
    </div>

    <div class="container" id="register-container" style="display: none;">
        <h2>註冊</h2>
        <form id="register-form">
            <input type="text" id="register-username" placeholder="會員名稱" required>
            <input type="password" id="register-password" placeholder="密碼" required>
            <button type="button" onclick="register()">註冊</button>
            <p class="toggle-link" onclick="toggleForm()">已經有會員了嗎? 點此登入</p>
        </form>
    </div>

    <script>
        const database = {};

        function toggleForm() {
            const loginContainer = document.getElementById('login-container');
            const registerContainer = document.getElementById('register-container');
            if (loginContainer.style.display === 'none') {
                loginContainer.style.display = 'block';
                registerContainer.style.display = 'none';
            } else {
                loginContainer.style.display = 'none';
                registerContainer.style.display = 'block';
            }
        }

        function login() {
            const username = document.getElementById('login-username').value;
            const password = document.getElementById('login-password').value;
            if (database[username] && database[username] === password) {
                alert('登入成功!');
            } else {
                alert('錯誤的會員名稱或密碼');
            }
        }

        function register() {
            const username = document.getElementById('register-username').value;
            const password = document.getElementById('register-password').value;
            if (database[username]) {
                alert('此會員名稱已被使用!');
            } else {
                database[username] = password;
                alert('註冊成功! 您現在可以登入了');
                toggleForm();
            }
        }
    </script>
</body>
</html>
